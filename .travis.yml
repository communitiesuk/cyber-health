language: generic
os: linux
dist: xenial
install: scripts/install-dependencies.sh
before_script: scripts/start-services.sh

deploy:
  # deploy develop to the staging environment
  - provider: script
    script: scripts/deploy.sh sandbox
    cleanup: false
    on:
      branch: develop
  # deploy master to production
  - provider: script
    script: scripts/deploy.sh sandbox
    cleanup: false
    on:
      branch: master

branches:
  only: 
  - master
  - develop
env:
  global: 
    - CLOUDFOUNDRY_PASSWORD=S220tEq9893Uw44Igsq
    - CLOUDFOUNDRY_USERNAME=joel.stobart@wearesnook.com
    - FRONTEND_PORT=8081
    - FRONTEND_HOST=localhost
    - FRONTEND_PROTO=http
    