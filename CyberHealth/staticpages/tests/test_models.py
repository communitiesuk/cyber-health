from django.test import TestCase
from staticpages.models import *

# tests for each model used by the staticpages app will go here

class SomeModelTestCase(TestCase):
  pass
  # def setUp(self):
    # self.some_object = SomeModel.objects.create()

  # def test_object_has_name(self):
  #   self.assertEqual("Section 1: MANAGE security risk", self.some_object.name)
